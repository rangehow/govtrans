Translate the following government text from {{source_language}}
({{source_language_code}}) to {{target_language}} ({{target_language_code}}) in
a single pass. No references, no glossary — just your best direct translation.
This is the evaluation baseline (never modified to include pipeline aids).

Reply with ONLY a JSON object: {"translation": "..."}

Source:
{{source_text}}
